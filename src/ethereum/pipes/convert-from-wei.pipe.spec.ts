import { ConvertFromWeiPipe } from './convert-from-wei.pipe';

describe('ConvertFromWeiPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertFromWeiPipe();
    expect(pipe).toBeTruthy();
  });
});
